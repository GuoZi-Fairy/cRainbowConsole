#include <windows.h>
#include "ccRainbow.h"
int main(int argc, char const *argv[])
{
    // char test[] = "{\"  text\":\"hello world\",\"red\"}";
    // HANDLE handle = GetStdHandle(STD_OUTPUT_HANDLE);
    // SetConsoleTextAttribute(handle, FOREGROUND_INTENSITY | FOREGROUND_RED);
    return 0;
}
